-- Migration 001: Baseline
-- This is a marker migration. The current database schema is already in place.
-- All future migrations will be numbered sequentially from here.

-- No changes needed - database already exists with current schema
SELECT 1;
